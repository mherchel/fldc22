{{ include('pubsub:sponsor-teaser', {
  href: node.field_website_address.uri,
  title: node.title.value,
  logo: content.field_sponsor_logo,
  body: content.body,
  title_suffix,
  attributes,
}, with_context = false) }}
