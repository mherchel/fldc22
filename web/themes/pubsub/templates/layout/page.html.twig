<div class="layout-container">
  <header class="site-header container">
    <a class="site-header--logo-link" href="/"><img src="{{ pubsub_path }}/images/fldc22-logo.svg" alt="Florida DrupalCamp Logo"></a>
    {{ page.primary_menu }}
    <div class="site-header__action">
      <div class="site-header__register">
        <a href="http://www.eventbrite.com">Register Now!</a>
        <img class="site-header__register-arrow" src="{{ pubsub_path }}/images/arrow-right-white.svg" alt="">
      </div>
      <button class="mobile-nav-button" data-drupal-selector="mobile-nav-button" aria-label="{{ 'Toggle Main Menu'|t }}" aria-controls="header-nav" aria-expanded="false">
        <span class="mobile-nav-button__label">{{ 'Menu'|t }}</span>
        <span class="mobile-nav-button__icon"></span>
      </button>
    </div>
  </header>

  {% if is_front %}
    {% include '@pubsub/includes/hero.twig' %}
    {% include '@pubsub/includes/stats.twig' %}
  {% endif %}

  {{ page.highlighted }}

  {{ page.help }}

  <main class="container grid">
    <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}
    {{ page.content }}
  </main>

  <footer class="site-footer" >
    {{ page.footer_top }}
    {{ page.footer_bottom }}
  </footer>

  <div class="overlay" data-drupal-selector="overlay"></div>
</div>{# /.layout-container #}
