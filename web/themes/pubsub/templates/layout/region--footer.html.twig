{%
  set classes = [
    'region',
    'region--' ~ region|clean_class,
    'container',
    'grid',
  ]
%}

{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
